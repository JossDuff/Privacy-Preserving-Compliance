# ── Blockchain RPC ──────────────────────────────────────────────────
# JSON-RPC endpoint for the target EVM chain.
# Used by: new-compliance-definition, publish
RPC_URL=

# Private key of the deployer/regulator account (hex, with 0x prefix).
# Used by: new-compliance-definition, publish
PRIVATE_KEY=

# Public address of the regulator (hex, with 0x prefix).
# Used by: new-compliance-definition (--regulator flag)
PUBLIC_KEY=

# ── IPFS ────────────────────────────────────────────────────────────
# IPFS HTTP API endpoint for uploading circuit source code.
# Used by: publish
# Default: http://localhost:5001
IPFS_RPC_URL=

# ── Contract verification ──────────────────────────────────────────
# When set, deployed contracts are automatically verified on the
# block explorer after deployment (passed to `forge create --verify`).
# Used by: new-compliance-definition, publish
ETHERSCAN_API_KEY=

# Optional override for the block explorer verification API URL.
# Use this for non-Etherscan explorers (e.g. Blockscout).
# Only has effect when ETHERSCAN_API_KEY is also set.
# Used by: new-compliance-definition, publish
VERIFIER_URL=
